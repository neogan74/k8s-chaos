# Backlog — 2026-05-17

Automated repository review. Branch reviewed: `e2e-f-pod-network-loss`.
Companion to the rolling `backlog.md` / `TODO.md`; this snapshot captures
problems found today and the proposed plan for next week (2026-05-18 → 2026-05-24).

---

## Problems found

### P0 — Critical / blocking

- **Host disk is full — builds and tests cannot run.**
  `df` reports `/System/Volumes/Data` at 100% capacity with only ~144Mi free.
  `go build ./...`, `go vet`, and `go test` all fail with
  `no space left on device` (Go build cache cannot write `vet.cfg` / `_pkg_.a`).
  Build/test green status is therefore **unverified**. Must reclaim space before
  any further work:
  - Remove committed coverage artifacts in repo root: `cover.out` (~141 KB),
    `coverage.out` (~75 KB), `coverage_new.out` (~40 KB).
  - Run `go clean -cache -testcache` and clear `/var/folders/.../go-build*`.
  - Inspect `bin/` (13 entries) for stale binaries.

- **Uncommitted in-progress work on `e2e-f-pod-network-loss`.**
  - `test/e2e/e2e_test.go`: 608 lines removed (pod-network-loss block extracted).
  - `test/e2e/pod_network_loss_test.go`: new, untracked (443 lines, full Ginkgo
    suite — basic, safety, validation, advanced scenarios).
  - `internal/controller/node_handlers_test.go`: gofmt struct-alignment only.
  - `test/e2e/README.md`: updated.
  Work looks complete but is not committed/PR'd and is unverified due to the
  disk issue above.

### P1 — Important

- **Stale coverage files tracked in repo root.** `cover.out`, `coverage.out`,
  `coverage_new.out` should be removed and added to `.gitignore`.
- **Incomplete audit logging.** `internal/controller/history.go:291` —
  `// TODO: Extract from request context` (experiment actor/user not captured;
  audit-logging item in TODO.md is only partially done).
- **Local branch sprawl.** 10 local branches incl. likely-merged
  (`node-cpu-stress`, `node-taint`, `pod-network-corruption`, `history-ttl`)
  and a mistyped Cyrillic branch name `вp-mgmnt`. Prune merged branches.
- **Unit-test coverage still below the 80% target** (open in TODO.md / backlog).
  No CI coverage-threshold gate yet, so regressions are silent.

### P2 — Minor

- Scaffold TODOs remain: `chaosexperiment_controller.go:93`, `cmd/main.go:201`
  (kubebuilder boilerplate / optional cert-manager) — harmless, low priority.
- Documentation drift: many root-level status docs (`E2E_FIXES_SUMMARY.md`,
  `E2E_TESTS_FIX.md`, `TODO.md` recent-completions log) duplicate ROADMAP/ADR
  content and are diverging.

---

## Plan for next week (2026-05-18 → 2026-05-24)

### Must do

1. **Unblock the toolchain** — reclaim disk, then verify a clean
   `make build test` / `go test ./...` baseline.
2. **Land pod-network-loss E2E** — commit the branch, run the new
   `pod_network_loss_test.go` suite, open a PR, merge. Closes the P0
   backlog item "Add E2E coverage for `pod-network-loss`".
3. **Repo hygiene** — delete root coverage artifacts, gitignore them,
   prune merged local branches.

### New features to implement

4. **`node-disk-fill` chaos action** — follow the ADR 0008 pattern
   (P1 in backlog; only remaining un-implemented node action).
5. **Pause/resume experiments** — `paused: true` spec field; controller
   skips reconciliation while paused and resumes cleanly.
6. **Capture experiment actor for audit logging** — resolve
   `history.go:291` by threading the requesting user from the
   admission/request context into history records.

### Stretch

7. **`network-partition` chaos action** — simulate splits between pod
   groups (P1, larger scope; spike/ADR if time-boxed).
8. **CI coverage-threshold gate** — fail builds on coverage drop; first
   step toward the 80% target.

---

## Actions taken during this review

- Removed stale untracked coverage artifacts: `cover.out`, `coverage.out`,
  `coverage_new.out` (already gitignored via `*.out`, regenerable).
- Cleared the Go build/test cache (`go clean -cache -testcache`),
  reclaiming ~1.2 GB (free space 122Mi → 1.3Gi).
- Confirmed `doc`, `node-cpu-stress`, `node-taint`, `pod-network-corruption`
  are merged into `main` and safe to prune (not deleted — left for owner).

## Residual blocker (needs human action)

Even after cache cleanup, `go build ./...` still fails with
`no space left on device`: the system volume is 416Gi/460Gi used with only
~1.3Gi free, insufficient to compile the controller-runtime/k8s.io
dependency graph. Build/test verification remains **blocked**.
Recommended manual steps (not safe to automate — risk to user data):
free system disk space (APFS snapshots, `~/Library/Caches`, Docker images,
old `bin/` toolchain ~426 MB), then re-run `make build test`.

## Suggested order

`disk fix → commit/merge pod-network-loss → repo hygiene →
node-disk-fill → pause/resume → audit actor → (stretch) network-partition / CI gate`
